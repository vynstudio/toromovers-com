# Redeploy marker

This marker commit was added on 2026-09-08 to trigger a fresh production deployment from the `main` branch after the Toro Movers universal ads funnel merge.

Expected production routes after deployment:

- `/lp/local-movers`
- `/thank-you`
