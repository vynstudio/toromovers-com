/**
 * TEMPORARY overlay. Do not treat as the final visual system.
 *
 * true  = illustrations on service cards, gallery, FAQ, blog (not a redesign)
 * false = restore original job photos on those surfaces
 *
 * Family-trust proof photo (`proof-customer.webp`) stays either way.
 * Original photos remain in `public/images/moves/` — do not delete them.
 */
export const VECTORS_ONLY = true;
